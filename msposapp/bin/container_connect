#!/bin/bash

#Install check
[ ! -f .shopcode ] && echo "Not installed." && exit 0

SHOPCODE="`cat .shopcode`"
[ ! -f .$SHOPCODE.container ] && echo "No running container." && exit 0

podman exec -it `cat .$SHOPCODE.container` su - tfsupport
