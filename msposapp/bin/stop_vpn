#!/bin/bash

[ "$1" != "" ] && CONTAINER="$1" && docker exec $CONTAINER systemctl stop ipsec && exit 0

# check for running container
if [ -f ./.container ]; then
  CONTAINER="`cat .container`"
else
  echo "No Running Container!"
  exit 1
fi

# disconnect
docker exec $CONTAINER systemctl stop ipsec
